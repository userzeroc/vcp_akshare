# 环境与基础配置
python-dotenv>=1.0.0
pydantic>=2.0.0        # 可用于后续更强的类型校验与配置管理

# 数据抓取与分析
tushare>=1.2.89
pandas>=2.0.0
numpy>=1.24.0

# 数据库驱动与 ORM 层
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0 # PostgreSQL 异步可考虑 asyncpg，目前使用同步
alembic>=1.13.0        # 数据库迁移工具

# 回测辅助与其它生态（根据后续扩展可能需要）
# backtrader>=1.9.78

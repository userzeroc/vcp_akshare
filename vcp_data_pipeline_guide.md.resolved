# Tushare 数据入库模块使用说明

本次修改在项目中构建了一套**高内聚低耦合**、支持**增量断点续传**与**幂等插入（Upsert）**的 Tushare 数据到 PostgreSQL 本地库的数据管道。

以下是如何配置及使用这套系统的完整说明。

---

## 1. 快速检查与测试环境

在进行任何真实网络请求前，建议您先跑一次本地测试，以此验证环境变量以及 PostgreSQL 数据库配置是否已经就绪：

```bash
# 激活虚拟环境
source .venv/bin/activate

# 运行测试脚本
python test_env.py
```

> [!TIP]
> `test_env.py` 中新增了 `test_data_layer()` 方法，它**不消耗 Tushare 积分**，利用 mock 数据完整的模拟了一遍："连接数据库 -> 建表检查 -> upsert入库 -> 查询验证" 的过程，如果通过测试则证明整套本地数据链路全部正常可用。

---

## 2. 数据库迁移 (Alembic)

项目的 `src/database/models` 下定义了所需的所有的表结构。每当模型文件发生改变时，需要利用 Alembic 将改变同步到 PostgreSQL 中。
目前已经生成了初始化版，建库前如果您尚未同步表，只需执行：

```bash
alembic upgrade head
```

> [!NOTE]
> `env.py` 已设定只管辖项目中注册的 ORM 模型表，即使库中还有同实例上的其他模块的表（如 n8n_chat_histories）也不会受到干扰。

---

## 3. 两种数据同步方式

系统提供了两种独立的数据抓取与入库脚本，内置限流器和异常重连机制，自动处理 Tushare 的各类限制。

### A: 历史数据全量初始化 (仅需执行一次)

第一次建库时，用于抓取过去 N 年的所有基础和日线数据。
> [!WARNING]
> 受限于 120积分的并发与请求限制，日线和复权数据会逐日慢慢拉取，可能需要耗时数小时。不过程序支持**断点续传**，若某天中断，下次重跑并不会产生重复数据或产生严重错误。

执行方式：
```bash
python -m src.data.sync.full_sync
```

### B: 每日增量更新（日常运维）

系统自带“增量自动探测”机制，脚本会自动读取本地每张表最新的时间，然后向 Tushare 查询**最新时间点之后**的数据补齐，极快。
建议使用 `crontab` 等定时任务工具在**每天收盘后（17:00以后）**自动设定执行。

执行方式：
```bash
python -m src.data.sync.daily_sync
```

---

## 4. 核心模块与文件梳理

若后续要增加新的接口和数据表，可参考目前代码规范直接在以下各层扩展：

### ORM 持久层 （修改表结构）
- **路径**：`src/database/models/`
- **规范**：直接继承 `Base` 类，增加新的类后，需要在此目录下的 `__init__.py` 中导出它。后续只管 `alembic revision --autogenerate -m 'xxx'` 和 `alembic upgrade head`，Alembic会自动完成表结构迁移。

### 抓取层 (数据溯源与重试)
- **路径**：`src/data/fetchers/tushare_client.py` 
- **规范**：单例 `client`，对 `client.query(...)` 做了统一的每分钟频次限制。如有权限报错或超出积分限制的问题，可在其中调整 `_CALL_INTERVAL` 等待秒数。

### 清洗与入库层 (核心操作机制)
- **路径**：`src/data/storage/` 
- **规范**：为了保证重复运行不报错（幂等），使用 `ON CONFLICT DO UPDATE` 执行更新。要定义新的 Upsert 逻辑只需给新表指定 `conflict_columns` (如由 ts_code 和 trade_date 为联合主键)，并实现对于脏数据的脱水/强制转换（`cleaner.py`）。

---

## 5. 开发调用参考

在后续策略回测模块（strategy/backtest）中，可通过如下简易语法调入本地数据：

```python
from src.database.session import get_session
from src.database.models import StockDaily

with get_session() as session:
    # 比如读取所有建设银行 (601939.SH) 2024年以后的日线数据：
    data = session.query(StockDaily).filter(
        StockDaily.ts_code == '601939.SH',
        StockDaily.trade_date >= '2024-01-01'
    ).all()
    
    for row in data:
        print(row.trade_date, row.close)
```
